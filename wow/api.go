package wow

/*
	World of Warcraft related API methods should go in here.
*/

// example/placeholder
func (c *WoWClient) Nothing() (*Character, error) {
	return nil, nil
	/*
		var character *Character

		err := c.get(profile(c.region, profileID, c.region.Int(), name), character)

		if nil != err {
			return nil, err
		}

		return character, nil
	*/
}
